// JS Header
// Necesita de changelanguaje.js

function ChangeLang() {
	// Funcion que cambia el lenguaje por el select
	changeLangText(document.getElementById('selectLanguage').options[selectLanguage.selectedIndex].value);
}